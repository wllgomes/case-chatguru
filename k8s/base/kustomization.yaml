apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: case-chatguru
    includeSelectors: false