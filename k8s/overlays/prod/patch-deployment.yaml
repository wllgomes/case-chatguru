apiVersion: apps/v1
kind: Deployment
metadata:
  name: case-chatguru
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: case-chatguru
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"